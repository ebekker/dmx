/*
** THIS IS A DESIGN-TIME ONLY CONFIGURATION SETTINGS FILE.
**
** It is used for EF Core dev-supporting tooling, such as creating and applying EF Core
** migrations this file is *REQUIRED* but the DEV LOCAL file (described below) is optional.
**
** You can override any settings here by creating a JSON file at the relative
** path `./_IGNORE/appsettings.LOCAL.json`, this is for DEV LOCAL configuration
** settings that DO NOT get checked in, such as developer-specific connection
** strings like the one below but replacing YOUR-USER-NAME with your username.
*/
{
  "ConnectionStrings": {
    "DMX": "Data Source=../_IGNORE/dmx-model.db"
  }
}
